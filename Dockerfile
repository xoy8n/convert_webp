# Generated by https://smithery.ai. See: https://smithery.ai/docs/config#dockerfile
# Multi-stage Dockerfile for convert-webp-mcp-server
# Builder stage: compile TypeScript and install dependencies
FROM node:lts-alpine AS builder

# Install build dependencies for sharp
RUN apk add --no-cache python3 make g++ vips-dev fftw-dev build-base

WORKDIR /app

# Copy package manifests and lock file
COPY package.json package-lock.json tsconfig.json ./

# Install dependencies
RUN npm ci --ignore-scripts

# Copy source and build
COPY src ./src
RUN npm run build

# Production stage: slim runtime
FROM node:lts-alpine AS runtime

# Install runtime dependencies for sharp
RUN apk add --no-cache vips fftw libstdc++

WORKDIR /app

# Copy dist and production dependencies
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules
COPY package.json ./

# Set NODE_ENV
ENV NODE_ENV=production

# Expose port if needed (not specified, MCP uses stdio)
# ENTRYPOINT
CMD ["node", "dist/index.js"]
